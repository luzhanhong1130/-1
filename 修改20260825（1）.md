# 修改记录 2026-08-25（1）

> 本次记录覆盖：阶段 4 内置 Web 登录对账 + DOM 抽取用量的全部实现，以及 15+ 处编译错误的修复。

## 实现概要
- WebCookieManager + WebExtractOrchestrator（localStorage/__NEXT_DATA__/fetch/DOM 三级抽取）
- BillingWebViewModel + BillingWebPanel（BottomSheet + 全屏 Dialog 双入口）
- DB v2→v3 Migration：remote_usage_snapshots 加 source/note
- RemoteUsageRepository：ingestWebBundle / markManualSnapshotReference
- WebRefreshPrefs：加密存储用户默认刷新模式偏好
- NavGraph 加 BillingWeb 子路由（全屏 Dialog）
- network_security_config：放行内网 HTTP（自建 One API）
- 2 个 P0 Bug 修复：WebView 滚动（requestDisallowInterceptTouchEvent）+ 路由 ClassCastException

## 验证
- kspDebugKotlin+assembleDebug 通过（仅 3 条 deprecation 警告）
- 模拟器 QA：BottomSheet/全屏 Dialog/WebView 滚动/抽取/保留参考视图均通过

---
软件签名：TRAE AI 开发环境
大模型签名：Trae 智能助手
