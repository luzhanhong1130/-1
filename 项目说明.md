# LLMHub 项目说明

## 项目简介

LLMHub 是一款多模型聚合聊天 Android 应用，支持统一管理多家大语言模型厂商的 API Key、配置模型参数、进行对话聊天，并提供用量统计与 Web 登录对账功能。

## 技术栈

| 分类 | 技术 |
|------|------|
| 开发语言 | Kotlin |
| UI 框架 | Jetpack Compose Material3 |
| 依赖注入 | Hilt |
| 本地数据库 | Room (KSP 生成代码) |
| 版本目录 | Gradle Version Catalog (libs.versions.toml) |
| 构建工具 | Gradle 8.9 / AGP 8.5.2 |
| 编译版本 | compileSdk 36 |
| 网络请求 | OkHttp / Retrofit 风格 Provider |
| 序列化 | Kotlinx Serialization |
| Web 能力 | Android WebView (用于厂商后台登录对账) |

## 项目目录结构

```
根目录/
├── app/                          # 应用主模块
│   ├── schemas/                  # Room 数据库 Schema 导出
│   │   └── com.llmhub.app.data.db.LlmHubDatabase/
│   │       ├── 1.json            # DB v1 Schema
│   │       ├── 2.json            # DB v2 Schema
│   │       └── 3.json            # DB v3 Schema (Web对账 Migration)
│   └── src/main/
│       ├── java/com/llmhub/app/
│       │   ├── data/             # 数据层
│       │   │   ├── billing/      # 用量抓取（Web对账抽取策略）
│       │   │   ├── db/           # Room 数据库与类型转换
│       │   │   ├── model/        # 数据实体类（聊天/配置/用量/价格）
│       │   │   ├── prefs/        # 安全存储与偏好设置
│       │   │   ├── remote/       # 厂商 Provider (OpenAI/Anthropic/Gemini/兼容)
│       │   │   └── repository/   # 仓库层 (Key/Chat/Model/Usage)
│       │   ├── di/               # Hilt 模块 (Database/Network)
│       │   └── ui/               # UI 层
│       │       ├── billing_web/  # Web登录对账面板
│       │       ├── chat/         # 聊天界面
│       │       ├── components/   # 通用组件 (Scaffold/Card)
│       │       ├── keys/         # API Key 管理
│       │       ├── models/       # 模型配置
│       │       ├── navigation/   # 导航图与路由
│       │       ├── stats/        # 用量统计页
│       │       └── theme/        # Compose 主题 (颜色/字体/形状)
│       │   ├── LLMHubApplication.kt   # Application 入口
│       │   └── MainActivity.kt         # 主 Activity
│       ├── res/                  # 资源文件
│       │   ├── drawable/         # 启动图标图层
│       │   ├── mipmap-anydpi-v26/ # 自适应启动图标
│       │   ├── values/           # 颜色/字符串/主题
│       │   └── xml/              # 网络安全配置/备份规则
│       └── AndroidManifest.xml   # 清单文件
│   ├── build.gradle.kts          # App 模块构建脚本
│   └── proguard-rules.pro        # 混淆规则
├── gradle/
│   ├── wrapper/                  # Gradle Wrapper
│   │   ├── gradle-wrapper.jar
│   │   └── gradle-wrapper.properties
│   └── libs.versions.toml        # 版本目录 (依赖版本统一管理)
├── qa_output/                    # QA 验证截图产物
├── .gitignore                    # Git 忽略规则
├── build.gradle.kts              # 顶层构建脚本
├── gradle.properties             # Gradle 属性
├── gradlew / gradlew.bat         # Gradle Wrapper 脚本
├── settings.gradle.kts           # Gradle 设置 (仓库镜像配置)
├── CHANGELOG.md                  # 版本变更日志
├── 项目说明.md                    # 本文档
├── 项目日志.md                    # 项目操作日志
├── 修改记录_*.md                  # 各轮修复与开发记录
├── 回归QA验证报告_*.md            # QA 回归验证报告
├── 跨平台方案设计_LLMHub_iOS+Android.md  # 跨平台设计文档
├── insert_key.sql                # Key 表样例数据 SQL
├── insert_model.sql              # Model 表样例数据 SQL
├── insert_usage.sql              # Usage 表样例数据 SQL
├── query.sql / query_*.sql       # 调试查询 SQL
└── models_card.png               # 模型配置页预览图
```

## 核心功能模块

### 1. API Key 管理 (`ui/keys`)
- 多厂商 Key 的增删改查
- 安全加密存储 (SecureKeyStore)
- Key 与模型配置的级联删除

### 2. 模型配置 (`ui/models`)
- 多模型参数配置（温度、最大 Token、Top-P 等）
- 支持 OpenAI 兼容 / Anthropic / Gemini 等 Provider
- ModelProvider 工厂模式动态创建请求客户端

### 3. 聊天界面 (`ui/chat`)
- 多轮对话、流式响应
- 消息持久化到 Room 数据库
- 发送失败自动补记错误信息

### 4. 用量统计 (`ui/stats`)
- 按模型 / 按日聚合 Token 用量
- 模型对比面板 (ComparePanel)
- 用量明细滚动区域

### 5. Web 登录对账 (`ui/billing_web`)
- 内置 WebView 登录厂商后台
- 支持 ONE_API / DEEPSEEK 自动抽取用量
- Cookie 持久化、按 Host 清理
- 抽取结果跳转到统计页进行对比

## 数据库设计

- **v1 → v2**: 基础聊天 / 配置 / 用量结构
- **v2 → v3**: 新增 WebRefreshPrefs 表（用于 Web 对账刷新间隔偏好），保留全量数据（Migration 实现）
- Schema 导出路径：`app/schemas/`（Room 编译时自动生成）

## 构建与运行

### 环境要求
- JDK 17+
- Android SDK (compileSdk 36)
- Gradle 8.9（通过 Wrapper 自动下载）

### 构建命令
```powershell
# Windows (使用系统 Gradle，当 Wrapper 脚本缺失时)
& "C:\Users\yuan1\.gradle\wrapper\dists\gradle-8.9-bin\...\gradle.bat" kspDebugKotlin --console=plain --no-daemon
& "C:\Users\yuan1\.gradle\wrapper\dists\gradle-8.9-bin\...\gradle.bat" assembleDebug --console=plain --no-daemon

# 或使用 Wrapper (如果已配置)
.\gradlew.bat assembleDebug
```

### 验证链路
1. `kspDebugKotlin`：Room 投影 / Schema 生成通过
2. `assembleDebug`：整体编译打包通过
3. 模拟器 QA：`android-emulator-qa` 技能验证 UI 流程无崩溃

## 项目约定

1. **文档与日志**：
   - `项目日志.md`：记录每次操作，只加不减，头部写入时间，尾部添加软件与大模型签名
   - `修改记录_YYYYMMDD（N）.md`：每轮修复 / 开发的详细记录
   - `回归QA验证报告_YYYYMMDD.md`：模拟器回归验证产物

2. **代码注释**：所有源文件必须包含注释，并在文件尾添加软件与大模型签名

3. **Git 提交**：通过 GitHub 插件分批提交，保留项目结构完整

## 关联仓库

当前代码托管于：`https://github.com/luzhanhong1130/-1.git`

---

**软件签名**：TRAE AI 开发环境
**大模型签名**：Trae 智能助手
